# Copy to config/devices.yaml (ignored by Git). Credentials are references, never passwords.
devices:
  - name: lab-ex4300
    management_ip: 192.0.2.10
    type: switch
    vendor: juniper
    site: lab
    credentials_reference_id: lab_juniper
    port: 22

